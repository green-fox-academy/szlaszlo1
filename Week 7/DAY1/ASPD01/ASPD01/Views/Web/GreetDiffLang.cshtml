@model ASPD01.Models.GreetDiffLang

<!DOCTYPE HTML>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Getting Started: Serving Web Content</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />

</head>
<body>
    <div>
    @foreach (string i in @Model.Hellos) {
        //color: rgba(@Model.GetRGB(), @Model.GetRGB(), @Model.GetRGB());
            <p style="font-size: @Model.GetSize();
                      color: rgba(@Model.GetRGB(), @Model.GetRGB(), @Model.GetRGB());
             ">
        @i, @Model.Name!</p>

    }
        </div>
</body>
</html>

